{
  "name": "trading-agent-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for the autonomous trading agent audit trail",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:testnet": "npx hardhat run scripts/deploy.js --network xltestnet",
    "deploy:mainnet": "npx hardhat run scripts/deploy.js --network xlmainnet"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5"
  }
}
